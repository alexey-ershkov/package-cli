# package-cli
A CLI to show and add dependencies to package.json

## Install
Via npm
```
$ npm i -g mail-package-cli
```

## Usage
Via npm
```
$ mail-package-cli
```
Via npx
```
$ npx mail-package-cli
```
